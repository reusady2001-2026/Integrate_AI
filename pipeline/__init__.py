"""Mechanical pipeline for turning a source document into a renderable draft.

The agent stays in the loop for the intelligent filling (see
methodology/drafting-playbook.md); this package only automates the mechanical
steps: extract source text, scaffold a draft from a template, render to RTL Word.
"""
